import { ResourceFormPage } from "../../components/ResourceFormPage";
import { guidesResource } from "../../services/resources/guidesResource";

const GuideFormPage = () => <ResourceFormPage resource={guidesResource} />;

export default GuideFormPage;
